{
  "name": "@fold/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run --filter @fold/client build && playwright test --project desktop --project mobile",
    "screenshot": "bun run --filter @fold/client build && playwright test --project screenshot",
    "favicons": "node build-favicons.mjs",
    "typecheck": "tsc --noEmit",
    "seed:history": "bun seed-history.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
