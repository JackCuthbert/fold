{
  "name": "@caldav-todo/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run --filter @caldav-todo/client build && playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.0"
  }
}
