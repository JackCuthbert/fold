{
  "name": "@fold/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:browsers": "playwright install --with-deps chromium",
    "test": "bun run --filter @fold/client build && playwright test --project desktop --project desktop-real --project mobile",
    "screenshot": "bun run --filter @fold/client build && playwright test --project screenshot",
    "favicons": "node build-favicons.mjs",
    "typecheck": "tsc --noEmit",
    "seed:history": "bun seed-history.mjs",
    "seed:all": "bun seed-all.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  }
}
