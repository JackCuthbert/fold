import DefaultTheme from 'vitepress/theme'
import type { Theme } from 'vitepress'
import './fold.css'

// The default theme with Fold's palette over the top — see fold.css.
const theme: Theme = DefaultTheme

export default theme
